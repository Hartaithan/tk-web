import { FC } from "react";

const MainHeader: FC = () => {
  return (
    <header className="flex items-center justify-center min-h-[48px]">
      <p className="uppercase font-bold">Карты</p>
    </header>
  );
};

export default MainHeader;
